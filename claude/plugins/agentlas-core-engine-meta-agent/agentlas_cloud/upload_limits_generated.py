# ⚠️ GENERATED FILE — do not hand-edit; the next generation erases your change.
# 정본: agentlas/AgentsAtlas/app/src/lib/agentlas-cloud/upload-scan-catalog.json
# 생성: (agentlas/AgentsAtlas/app) node scripts/gen-upload-scan-catalog.mjs
#
# Package ceilings. The wall is the store, not the network: a package's bytes
# ride inside one manifest document (16 MiB BSON cap) as base64, so the
# document carries 4/3 of PACKAGE_MAX_TOTAL_BYTES. Six surfaces used to state
# these by hand; raising one and not the others turns into a refusal on the
# next surface with a code and no file name attached.

PACKAGE_MAX_TOTAL_BYTES = 31457280
PACKAGE_MAX_FILE_BYTES = 6291456
PACKAGE_MAX_UNCOMPRESSED_TOTAL_BYTES = 125829120
PACKAGE_MAX_UNCOMPRESSED_FILE_BYTES = 25165824
PACKAGE_MAX_FILES = 400
PACKAGE_MAX_REQUEST_BYTES = 47185920
