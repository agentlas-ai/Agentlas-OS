description = "Sign this machine into Agentlas (opens the browser sign-in window)."
prompt = """
# /agentlas-login

Argument: {{args}}

Identical to `/hep-login` and `/agentlas login <request>`. Locate the file named `hep-login.toml` in the exact same directory this file was loaded from, read its `prompt` value, and follow those instructions exactly — treating the argument above as that command's request.

Do not improvise a separate workflow and do not summarize `hep-login.toml` from memory; that file is the sole authority for this command's behavior.
"""
