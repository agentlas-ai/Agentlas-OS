description = "Build, repair, or package Agentlas agents and teams with Hephaestus."
prompt = """
# /agentlas-build

Argument: {{args}}

Identical to `/hep-build` and `/agentlas build <request>`. Locate the file named `hep-build.toml` in the exact same directory this file was loaded from, read its `prompt` value, and follow those instructions exactly — treating the argument above as that command's request.

Do not improvise a separate workflow and do not summarize `hep-build.toml` from memory; that file is the sole authority for this command's behavior.
"""
