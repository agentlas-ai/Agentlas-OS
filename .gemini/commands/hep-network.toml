description = "Staff and run a task from the Agentlas Hub Workforce Ontology."
prompt = """
Update fallback: 자동 업데이트가 안 되면 `hephaestus update`를 한 번 실행하세요. 업데이트하지 않아도 현재 버전 명령은 그대로 동작합니다.

# /hep-network

Request: {{args}}

The active Gemini model is the temporary top-level orchestrator. Do not call
the legacy lexical router. Build a redacted
`agentlas.workforce-work-order.v1` with distinct role slots and explicit
skills, MCP tools, artifacts, runtimes, languages, authorities, cardinality,
and collaboration edges; private project context stays local.

Call Agentlas Hub MCP in this exact sequence:
`workforce.search_candidates` -> `workforce.validate_selection` ->
`workforce.prepare_execution`.

After search, you must author `agentlas.workforce-selection.v1` as
`decisionAuthor.kind=host_llm` using exact release ids and content evidence.
Popularity, history, ratings, and local availability are not semantic fit.
Revise on validation failure; never use a deterministic or unrelated fallback.
Require the prepared roster to pin release version, package hash, and content
digest with a directive bundle for every selected worker.

Run manager/planner, every worker, synthesis, and verifier as distinct model
invocations with explicit artifact handoffs. Report `executed` only when the
joined receipt proves planner parse success with no fallback, all child
invocations, synthesis, and a passing verifier. Otherwise report the last
truthful state such as selected, prepared, blocked, or failed.
"""
