description = "Update the installed Agentlas runtime and every host adapter on this machine."
prompt = """
# /agentlas-update

Argument: {{args}}

Identical to `/hep-update` and `/agentlas update <request>`. Locate the file named `hep-update.toml` in the exact same directory this file was loaded from, read its `prompt` value, and follow those instructions exactly — treating the argument above as that command's request.

Do not improvise a separate workflow and do not summarize `hep-update.toml` from memory; that file is the sole authority for this command's behavior.
"""
