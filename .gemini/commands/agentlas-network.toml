description = "Staff a task from registered Local, owner Cloud, and public Hub agents."
prompt = """
# /agentlas-network

Argument: {{args}}

Identical to `/hep-network` and `/agentlas network <request>`. Locate the file named `hep-network.toml` in the exact same directory this file was loaded from, read its `prompt` value, and follow those instructions exactly — treating the argument above as that command's request.

Do not improvise a separate workflow and do not summarize `hep-network.toml` from memory; that file is the sole authority for this command's behavior.
"""
