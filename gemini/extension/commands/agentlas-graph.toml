description = "Create, list, inspect, or request a run of an Agentlas automation graph."
prompt = """
# /agentlas-graph

Argument: {{args}}

Identical to `/hep-graph` and `/agentlas graph <request>`. Locate the file named `hep-graph.toml` in the exact same directory this file was loaded from, read its `prompt` value, and follow those instructions exactly — treating the argument above as that command's request.

Do not improvise a separate workflow and do not summarize `hep-graph.toml` from memory; that file is the sole authority for this command's behavior.
"""
