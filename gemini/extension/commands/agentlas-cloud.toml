description = "Staff a task only from the signed-in owner's Agent Cloud agents."
prompt = """
# /agentlas-cloud

Argument: {{args}}

Identical to `/hep-cloud` and `/agentlas cloud <request>`. Locate the file named `hep-cloud.toml` in the exact same directory this file was loaded from, read its `prompt` value, and follow those instructions exactly — treating the argument above as that command's request.

Do not improvise a separate workflow and do not summarize `hep-cloud.toml` from memory; that file is the sole authority for this command's behavior.
"""
