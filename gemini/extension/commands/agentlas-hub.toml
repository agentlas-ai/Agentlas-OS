description = "Staff a task only from public Agentlas Hub agents."
prompt = """
# /agentlas-hub

Argument: {{args}}

Identical to `/hep-hub` and `/agentlas hub <request>`. Locate the file named `hep-hub.toml` in the exact same directory this file was loaded from, read its `prompt` value, and follow those instructions exactly — treating the argument above as that command's request.

Do not improvise a separate workflow and do not summarize `hep-hub.toml` from memory; that file is the sole authority for this command's behavior.
"""
