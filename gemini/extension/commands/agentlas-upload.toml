description = "Upload an Agentlas agent after asking Cloud vs Hub first."
prompt = """
# /agentlas-upload

Argument: {{args}}

Identical to `/hep-upload` and `/agentlas upload <request>`. Locate the file named `hep-upload.toml` in the exact same directory this file was loaded from, read its `prompt` value, and follow those instructions exactly — treating the argument above as that command's request.

Do not improvise a separate workflow and do not summarize `hep-upload.toml` from memory; that file is the sole authority for this command's behavior.
"""
